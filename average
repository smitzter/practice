print("the average temperature for the week was:")
      
def mean(numbers):
    return float(sum(numbers)) / max(len(numbers), 1)

print(mean([83, 88, 93, 90, 90, 92, 92]))

#or even easier

a = [1, 2, 3, 4, 5, 6]
print(sum(a) / len(a))
