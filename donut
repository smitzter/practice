day = input("What day of the week is it?")
date = input("What day of the month is it?")

donut25 = float(12 - (12 * .99 * .25))
donut = float(12 * .99)

if day == ("Sunday"):
    if date % 2 == 0:
        print(donut25)
else:
    print(donut)
